instruments {
    WavPlayer {
        perf {
            local left, right: Audio = WavPlayer("C:/Users/ryand/Downloads/Suspirium1 mono.mp3");
            output(left, right);
        }
    }
}

score {
    WavPlayer(0.0 10.0);
}