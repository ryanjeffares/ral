instruments {
    WavPlayer {
        perf {
            local left, right: Float = WavPlayer("C:/Users/ryand/Downloads/Suspirium1.wav");
            output(left, right);
        }
    }
}

score {
    WavPlayer(0.0 10.0);
}