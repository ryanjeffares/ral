instruments {
    WavPlayer {
        perf {
            local audio: Audio = WavPlayer("C:/Users/ryand/Downloads/Suspirium1.wav");
            output(audio, audio);
        }
    }
}

score {
    WavPlayer(0.0 10.0);
}