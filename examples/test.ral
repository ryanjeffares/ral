instruments {
    Sine {
        frequency: Float;

        init(freq: Float) {
            frequency = freq;
            print("Frequency: ");
            println(frequency);
        }

        perf(amps: Float) {
            println(amps);         
        }
    }
}

score {
    Sine(0.0 1.0 init(440.0) perf(1.0));
}